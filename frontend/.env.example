# Backend API base URL (leave empty to use Vite proxy, or set to full URL in production)
VITE_API_BASE_URL=

# Set to "true" to use mock data without a real backend (for local development)
VITE_USE_MOCK=true
